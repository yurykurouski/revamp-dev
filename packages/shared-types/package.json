{
  "name": "@revamp/shared-types",
  "version": "0.1.0",
  "description": "Shared TypeScript types and interfaces for Revamp SaaS",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
